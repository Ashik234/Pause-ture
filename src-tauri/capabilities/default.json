{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window and reminder popup",
  "windows": ["main", "reminder"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "opener:default"
  ]
}
